# Binaries for programs and plugins
*.exe
*.exe~
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Gofra project binary output
bin/**
